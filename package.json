{
  "name": "progetto_ids",
  "version": "1.0.0",
  "description": "- Difficoltà di comunicazione col comune per i cittadini\r  - Difficoltà per il comune di selezione delle segnalazioni da sistemare",
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.7.3"
  },
  "scripts": {
    "build": "npm i && npm run build -workspace=vue-project",
    "start": "node index.js",
    "dev": "npm run build -workspace=vue-project && node -r dotenv/config index.js",
    "setup": "node -r dotenv/config scripts/testSetup.js",
    "demo": "node -r dotenv/config scripts/setupDemo.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "vue-project"
  ]
}